
<div class="show-body">
    <div class="more-info">
    <h1>Leave your review about <strong><%= @teacher.name%></h1></strong>
    </div>
      <div class="show">
   <%= simple_form_for [@teacher, @review] do |f| %>
          <%= f.input :comment %>
          <div class="buttons">
          <p class="btn btn-blue"><%= f.submit %></p>
          </div>
        <% end %>
        </div>
      </div>
